stages:
  #- test
  - deploy

# w3c-html-test:
#   stage: test
#   image: docker:26.1.2-dind-alpine3.19
#   script:
#     - echo "testing..."

deploy:
  stage: deploy
  image: alpine:latest
  variables:
    USER: root
    HOST: lav15.ventsdouest.dev
    HOST_IP: 51.210.243.20
  before_script:
    - 'command -v ssh-agent >/dev/null || ( apk add --update openssh)' 
    - eval $(ssh-agent -s)
    - mkdir -p ~/.ssh
    - chmod 700 ~/.ssh
    - echo "$SSH_PRIVATE_KEY" | base64 -d > ~/.ssh/id_rsa
    - echo $HOST_IP >> ~/.ssh/known_hosts
  script:
    - echo "Pulling repository..."
    - ssh $USER@$HOST "cd /docker/sae/data/web && git pull"
    - echo "Pulled"
  #when: manual
  #only:
  #  refs:
  #    - main